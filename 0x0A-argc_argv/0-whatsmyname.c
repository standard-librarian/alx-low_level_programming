#include <stdio.h>

/**
 * main - check the code
 * @argc: counter of the cli args
 * @argv: the vector of the cli args
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
